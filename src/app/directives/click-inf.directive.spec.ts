import { ClickInfDirective } from './click-inf.directive';

describe('ClickInfDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickInfDirective();
    expect(directive).toBeTruthy();
  });
});
